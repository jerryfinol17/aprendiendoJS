import {config} from '../config'
import {LOCATORS} from '../config'
import {CHECKOUT_DATA} from "../config";
export const BASE_URL = config.BASE_URL
export const CREDENTIALS = config.CREDENTIALS;
export { LOCATORS };
export {CHECKOUT_DATA};
